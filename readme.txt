2.0实现了 
（1）绑定ip已经不是127.0.0.1了而是本机的真实ip
（2）点击start： 显示本机绑定的ip：port(如果取ip成功的话)
                         显示当前服务器的状态是 running（如果绑定成功的话），否则stop

问题：重复start会导致绑定不成功 提示不成功，显示stop（但实际服务器有没有在
运行我不知道，应该要测试一下）


------------------------------------------------------------------------------------
2.1 将要实现：

server start 的listen
break按钮的触发
client端的对接
整合后再考试上述问题，看看if else 应该放在哪才能允许重复start 
感觉是这个start已经成为实际意义的restart了。

------------------------------------------------------------------------------------
3.0 目前：

显示成功连接
显示接受的内容文本

------------------------------------------------------------------------------------
3.1目前：

删掉了不必要的receive 函数，用内置的recv代替

-----------------------------------------

4.0 实现了：

连接断开可重复性
异步非阻塞 实时文本发送、接受

5.0打算可以

读取文件夹图像
显示读取的图像
传输图像





